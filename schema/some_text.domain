-- Domain: some_text

-- DROP DOMAIN some_text;

CREATE DOMAIN some_text
  AS text
  COLLATE pg_catalog."default"
  CONSTRAINT some_text_check CHECK (char_length(VALUE) > 0);
ALTER DOMAIN some_text
  OWNER TO postgres;

